pipeline {
    agent any

    stages {
        stage('1') {
            steps {              
                echo 'commit'
                git commit -am "add archivo jenkins"
            }
        }
         
    }
}
