pipeline {
    agent any

    stages {
        stage('1') {
            steps {              
                echo 'commit'
                sh 'git commit -m "add archivo jenkins"'
                
            }
        }
         
    }
}
